<div class="bg-white border border-slate-200 rounded-3xl shadow-flat p-6 h-full flex flex-col">
    <div class="mb-4">
        <h3 class="text-base font-bold text-slate-900">{{ title }}</h3>
        {% if description %}
        <p class="text-xs text-slate-500 mt-1 font-medium">{{ description }}</p>
        {% endif %}
    </div>
    <div class="relative flex-grow min-h-[300px]">
        <canvas id="{{ canvas_id }}"></canvas>
    </div>
</div>
