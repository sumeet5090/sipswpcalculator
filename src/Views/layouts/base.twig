<!DOCTYPE html>
<html lang="en-IN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <meta name="color-scheme" content="light">
    <title>{{ page_config.title|default('SIP & SWP Calculator') }}</title>
    <meta name="description" content="{{ page_config.meta_desc|default('Free SIP & SWP calculator with step-up compounding.') }}">
    {% if page_config.keywords is defined %}
    <meta name="keywords" content="{{ page_config.keywords }}">
    {% endif %}
    
    {% set current_uri = request.server('REQUEST_URI', '/') %}
    {% set canonical_url = page_config.canonical|default(site_url ~ current_uri) %}
    <link rel="canonical" href="{{ canonical_url }}">
    <meta name="robots" content="{{ page_config.robots|default('index, follow') }}">

    <meta name="author" content="{{ page_config.author|default('Sumeet Boga') }}">

    <link rel="alternate" hreflang="en-IN" href="{{ canonical_url }}">
    <link rel="alternate" hreflang="x-default" href="{{ canonical_url }}">

    <meta property="og:type" content="{{ page_config.og_type|default('website') }}">
    <meta property="og:site_name" content="SIP & SWP Calculator">
    <meta property="og:locale" content="en_IN">
    <meta property="og:url" content="{{ canonical_url }}">
    <meta property="og:title" content="{{ page_config.og_title|default(page_config.title|default('SIP & SWP Calculator')) }}">
    <meta property="og:description" content="{{ page_config.og_desc|default(page_config.meta_desc|default('Free SIP & SWP calculator with step-up compounding.')) }}">
    <meta property="og:image" content="{{ page_config.og_image|default(site_url ~ '/assets/og-image-main.jpg') }}">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">

    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:creator" content="{{ page_config.twitter_creator|default('@sumeetboga') }}">
    <meta name="twitter:title" content="{{ page_config.og_title|default(page_config.title|default('SIP & SWP Calculator')) }}">
    <meta name="twitter:description" content="{{ page_config.og_desc|default(page_config.meta_desc|default('Free SIP & SWP calculator with step-up compounding.')) }}">
    <meta name="twitter:image" content="{{ page_config.og_image|default(site_url ~ '/assets/og-image-main.jpg') }}">

    <link rel="icon" type="image/svg+xml" href="/assets/favicon.svg">
    <link rel="manifest" href="/manifest.json">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet" media="print" onload="this.media='all'">
    <noscript>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    </noscript>

    {# ── Vite Asset Integration: HMR client (dev) & compiled CSS links (prod) ── #}
    {{ vite_client() }}
    {{ vite_css('resources/js/app.ts') }}

    {{ page_config.additional_head|default('')|raw }}

    {% if env == 'production' %}
        <!-- Sentry Error Monitoring -->
        <script src="https://js-de.sentry-cdn.com/475d8af2dda1d5fc4e23fd8923e5f908.min.js" crossorigin="anonymous"></script>

        <!-- Google tag (gtag.js) -->
        <script async src="https://www.googletagmanager.com/gtag/js?id=G-P5PLFTW4E7"></script>
        <script>
            window.dataLayer = window.dataLayer || [];
            function gtag() { dataLayer.push(arguments); }
            gtag('js', new Date());
            gtag('config', 'G-P5PLFTW4E7');
        </script>

        <!-- Ahrefs Webmaster Analytics -->
        <script src="https://analytics.ahrefs.com/analytics.js" data-key="WiDGDiqV9F0xelXDCYFUfw" async></script>

        <!-- Microsoft Clarity -->
        <script type="text/javascript">
            (function(c,l,a,r,i,t,y){
                c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
                t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
                y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
            })(window, document, "clarity", "script", "y1ed7um9dp");
        </script>
    {% endif %}
    
    {% block head %}{% endblock %}
</head>
<body class="{{ page_config.body_class|default('bg-gray-50 text-gray-800 font-sans antialiased') }} overflow-x-hidden" style="{{ page_config.body_style|default('background-image: var(--gradient-surface); background-attachment: fixed;') }}">
    <a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:p-3 focus:bg-emerald-600 focus:text-white focus:rounded-lg focus:shadow-xl focus:z-[100] font-bold transition-all">Skip to main content</a>
    
    {% include 'layouts/header.twig' %}
    
    <!-- spacer to prevent content from being hidden behind fixed header -->
    <div class="h-16"></div>
    
    <main id="main-content" class="container mt-6 mx-auto px-4 sm:px-6 lg:px-8 {{ page_config.container_class|default('max-w-7xl') }}">
        {% block content %}{% endblock %}
    </main>

    {% include 'layouts/footer.twig' %}

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // Mobile Menu Toggle
            var btn = document.getElementById('mobile-menu-btn');
            var menu = document.getElementById('mobile-menu');
            var hamburger = document.getElementById('hamburger-icon');
            var closeIcon = document.getElementById('close-icon');
            if (btn && menu) {
                btn.addEventListener('click', function () {
                    var isOpen = !menu.classList.contains('hidden');
                    menu.classList.toggle('hidden');
                    hamburger.classList.toggle('hidden');
                    closeIcon.classList.toggle('hidden');
                    btn.setAttribute('aria-expanded', !isOpen);
                });
            }
        });
    </script>
    <script type="module" src="{{ vite_asset('resources/js/app.ts') }}"></script>
    
    {% if page_config.scripts is defined %}
        {% for script in page_config.scripts %}
            <script src="{{ script }}" defer></script>
        {% endfor %}
    {% endif %}
    
    <!-- Ergonomic Mobile Sticky Action Dock (Visible only on <sm viewports) -->
    <div id="mobile-action-dock" class="sm:hidden fixed bottom-0 left-0 right-0 z-40 bg-white/95 backdrop-blur-md border-t border-slate-200/90 shadow-[0_-4px_16px_rgba(0,0,0,0.06)] px-4 py-2 flex items-center justify-around gap-2 pb-[max(0.5rem,env(safe-area-inset-bottom))]">
        <button type="button" onclick="document.getElementById('openPdfModalBtn')?.click()"
            class="flex-1 flex flex-col items-center justify-center py-1.5 px-2 rounded-xl bg-slate-50 hover:bg-slate-100 border border-slate-200 text-slate-700 text-[11px] font-bold active:scale-95 transition-all cursor-pointer">
            <span class="text-xs">📄</span>
            <span>PDF Plan</span>
        </button>
        <button type="button" onclick="document.getElementById('shareCalcBtn')?.click()"
            class="flex-1 flex flex-col items-center justify-center py-1.5 px-2 rounded-xl bg-emerald-50 hover:bg-emerald-100 border border-emerald-200 text-emerald-800 text-[11px] font-bold active:scale-95 transition-all cursor-pointer">
            <span class="text-xs">🔗</span>
            <span>Share</span>
        </button>
        <button type="button" onclick="document.getElementById('snapshot-scenario-btn')?.click()"
            class="flex-1 flex flex-col items-center justify-center py-1.5 px-2 rounded-xl bg-slate-50 hover:bg-slate-100 border border-slate-200 text-slate-700 text-[11px] font-bold active:scale-95 transition-all cursor-pointer">
            <span class="text-xs">📸</span>
            <span>Snapshot</span>
        </button>
        <button type="button" onclick="document.getElementById('calculator-section')?.scrollIntoView({behavior: 'smooth'})"
            class="flex flex-col items-center justify-center py-1.5 px-3 rounded-xl bg-slate-100 hover:bg-slate-200 text-slate-600 text-[11px] font-bold active:scale-95 transition-all cursor-pointer"
            aria-label="Scroll to calculator inputs">
            <span class="text-xs">↑</span>
            <span>Top</span>
        </button>
    </div>

    {% include 'components/command-palette.twig' %}
    {% include 'components/tax-waterfall-modal.twig' %}
    {% include 'components/goal-commitment-modal.twig' %}
    {% include 'components/qr-share-modal.twig' %}
    {% block modals %}{% endblock %}
    {% block scripts %}{% endblock %}
</body>
</html>
