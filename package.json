{
  "scripts": {
    "css:build": "tailwindcss --input src/input.css --output dist/tailwind.min.css --minify",
    "css:watch": "tailwindcss --input src/input.css --output dist/tailwind.min.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^4.2.0"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.2.0"
  }
}