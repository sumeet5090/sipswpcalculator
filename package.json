{
  "scripts": {
    "css:build": "tailwind --input src/input.css --output dist/tailwind.min.css --minify",
    "css:watch": "tailwind --input src/input.css --output dist/tailwind.min.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^4.2.0"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.2.0"
  }
}